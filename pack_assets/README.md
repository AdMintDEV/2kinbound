# Pack assets (not published)

This folder is **outside** the GitHub Pages publish root (`docs/`). The Pages workflow uploads `docs/` only.

Upload `4130-catalog.csv` (and optional `PACK_NOTES.md`) to the Payhip digital product. Do **not** copy these files back into `docs/`.
